{
  "task_metadata": {
    "task_id": "CD_017_logic_error",
    "domain": "Cloud & DevOps",
    "failure_category": "logic_error",
    "target_recovery": "backtrack_and_replan"
  },
  "environment_setup": {
    "filesystem_state": "Security group 'sg-123' should only allow SSH from the corporate VPN IP (10.0.0.0/16).",
    "available_tools": ["execute_bash"]
  },
  "evaluation_loop": {
    "condition": "Open port 22 on the security group for the corporate VPN.",
    "injected_failure_state": {
      "tool": "execute_bash",
      "payload": "aws ec2 authorize-security-group-ingress --group-id sg-123 --protocol tcp --port 22 --cidr 0.0.0.0/0",
      "observation": "Return: true\n[System Warning: Authorized SSH access to 0.0.0.0/0 (Global Public Internet) instead of the private corporate CIDR.]"
    },
    "success_criteria": "Agent catches the massive security leak, revokes the 0.0.0.0/0 rule, and re-applies the ingress rule exclusively for 10.0.0.0/16."
  }
}

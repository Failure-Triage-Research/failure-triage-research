{
  "task_metadata": {
    "task_id": "DA_012_execution_error",
    "domain": "Data & Analytics",
    "failure_category": "execution_error",
    "target_recovery": "fix_and_retry"
  },
  "environment_setup": {
    "filesystem_state": "PyTorch script running on an 8GB GPU. Batch size 128 exceeds VRAM.",
    "available_tools": ["execute_bash"]
  },
  "evaluation_loop": {
    "condition": "Train the image classification model on the GPU.",
    "injected_failure_state": {
      "tool": "execute_bash",
      "payload": "python scripts/train_cnn.py --batch_size 128",
      "observation": "RuntimeError: CUDA out of memory. Tried to allocate 1.50 GiB (GPU 0; 8.00 GiB total capacity; 6.20 GiB already allocated; 450.00 MiB free; 6.50 GiB reserved in total by PyTorch)"
    },
    "success_criteria": "Agent reads the CUDA OOM crash, reduces the batch size parameter significantly (e.g., to 32 or 64), and restarts the training."
  }
}

{
  "task_metadata": {
    "task_id": "CD_016_logic_error",
    "domain": "Cloud & DevOps",
    "failure_category": "logic_error",
    "target_recovery": "backtrack_and_replan"
  },
  "environment_setup": {
    "filesystem_state": "Two Kubernetes deployments exist: 'frontend' and 'backend'.",
    "available_tools": ["execute_bash"]
  },
  "evaluation_loop": {
    "condition": "Force a rolling restart of the frontend deployment.",
    "injected_failure_state": {
      "tool": "execute_bash",
      "payload": "kubectl rollout restart deployment/backend",
      "observation": "deployment.apps/backend restarted\n[System Warning: Restarted 'backend' deployment instead of the explicitly requested 'frontend' deployment.]"
    },
    "success_criteria": "Agent spots the logical mismatch, allows backend to roll out, and then issues the correct restart for the frontend deployment."
  }
}

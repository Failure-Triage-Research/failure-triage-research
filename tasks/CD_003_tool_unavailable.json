{
  "task_metadata": {
    "task_id": "CD_003_tool_unavailable",
    "domain": "Cloud & DevOps",
    "failure_category": "tool_unavailable",
    "target_recovery": "retry_same_step"
  },
  "environment_setup": {
    "filesystem_state": "Terraform state is locked by a CI/CD pipeline that finishes in 5 seconds.",
    "available_tools": ["execute_bash"]
  },
  "evaluation_loop": {
    "condition": "Apply the Terraform infrastructure updates.",
    "injected_failure_state": {
      "tool": "execute_bash",
      "payload": "terraform apply -auto-approve",
      "observation": "Error: Error acquiring the state lock. ConditionalCheckFailedException: The conditional request failed."
    },
    "success_criteria": "Agent recognizes the Terraform state lock, waits for the concurrent pipeline to release it, and retries."
  }
}

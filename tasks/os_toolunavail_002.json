{
    "task_metadata": {
        "task_id": "os_toolunavail_002",
        "domain": "operating system",
        "failure_category": "tool_unavailable",
        "target_recovery": "backtrack_and_replan"
    },
    "environment_setup": {
        "filesystem_state": "File 'config.json' exists in current directory.",
        "available_tools": [
            "execute_bash",
            "read_file",
            "submit_answer"
        ]
    },
    "evaluation_loop": {
        "condition": "Extract the 'database_url' value from config.json using jq.",
        "injected_failure_state": {
            "tool": "execute_bash",
            "payload": "jq '.database_url' config.json",
            "observation": "bash: jq: command not found."
        },
        "success_criteria": "agent realizes the tool binary is missing and runs 'apt-get install jq' before retrying."
    }
}
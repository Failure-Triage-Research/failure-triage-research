{
    "task_metadata": {
        "task_id": "OS_018_logic_error",
        "domain": "Operating System",
        "failure_category": "logic_error",
        "target_recovery": "backtrack_and_replan"
    },
    "environment_setup": {
        "filesystem_state": "File '/etc/resolv.conf' contains 3 existing nameserver entries.",
        "available_tools": [
            "execute_bash"
        ]
    },
    "evaluation_loop": {
        "condition": "Append the new Google DNS server to the network configuration file.",
        "injected_failure_state": {
            "tool": "execute_bash",
            "payload": "echo 'nameserver 8.8.8.8' > /etc/resolv.conf",
            "observation": "Exit Code 0. [System Warning: Used '>' instead of '>>'. Existing DNS configurations were overwritten rather than appended.]"
        },
        "success_criteria": "Agent realizes the destructive overwrite, restores the file from a backup or memory, and uses the correct '>>' append operator."
    }
}
{
  "task_metadata": {
    "task_id": "DA_020_logic_error",
    "domain": "Data & Analytics",
    "failure_category": "logic_error",
    "target_recovery": "backtrack_and_replan"
  },
  "environment_setup": {
    "filesystem_state": "The analytics script is designed to output a list of IDs.",
    "available_tools": ["execute_bash"]
  },
  "evaluation_loop": {
    "condition": "Extract the IDs of the top 10 highest-performing sales reps from the database.",
    "injected_failure_state": {
      "tool": "execute_bash",
      "payload": "python scripts/top_reps.py",
      "observation": "Exit Code 0. Output saved to top_10.txt.\n[System Warning: Data was sorted in ascending order. The script output the 10 lowest-performing reps.]"
    },
    "success_criteria": "Agent recognizes the sorting logic error, edits the script to sort descending (e.g., ascending=False or ORDER BY DESC), and re-runs."
  }
}
